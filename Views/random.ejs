<%- include('partials/head') %> 
<%- include('partials/navbar') %> 
    <!-- rand  is accesible; also, the js associated with it runs and displays result  -->
    <h1>Your random number is: <%= num %></h1>
    <!-- below we are imbedding logic that renders a different h2 tag if it's an even or odd number -->
    <% if(num % 2 === 0){ %>
    <h2>That is an even number!</h2>
    <% } else { %> 
    <h2>That is an odd number!</h2>
    <% } %>
    <!-- using ternary operator to imbed -->
    <h3>That number is: <%= num%2===0 ? 'EVEN' : 'ODD' %> </h3>

<%- include('partials/footer') %> 